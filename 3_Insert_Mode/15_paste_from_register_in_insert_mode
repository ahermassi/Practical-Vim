<C-r>0 -> paste the yanked text at the current cursor position in Insert Mode.

The general format of the command is <C-r>{register}, where {register} is the
address of the register we want to insert.

<C-r><C-p>{register} -> insert text literally and fixe any unintended indentation.
