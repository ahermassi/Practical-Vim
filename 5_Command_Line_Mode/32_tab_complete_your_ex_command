Just like in the shell, we can use the <Tab> key to autocomplete 
commands at the prompt.

<C-d> -> ask Vim to reveal a list of possible completions. 
If we hit the <Tab> key, the prompt will cycle through the options.
