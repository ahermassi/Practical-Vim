In Visual mode, we start off by making the selection and then trigger the change command.

viw -> visually select the word.

In a typical text editing environment, selected text is deleted when we type any
printable character. Vim’s Visual mode doesn’t follow this convention—but Select
mode does. We can toggle between Visual and Select modes by pressing <C-g> . 
